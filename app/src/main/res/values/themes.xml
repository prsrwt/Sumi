<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Sumi" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- A see-through window: the composer draws its own dim and sheet over
         whatever was on screen, the home screen included. -->
    <style name="Theme.Sumi.Composer" parent="Theme.Sumi">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <!-- The sheet animates itself; a window fade on top would double it. -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
